services:
  nivara:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./data/evaluation:/app/data/evaluation
      - audit-data:/app/data/audit

volumes:
  audit-data:
