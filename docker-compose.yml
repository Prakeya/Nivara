services:
  nivara:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
    volumes:
      - ./data/evaluation:/app/data/evaluation
      - audit-data:/app/data/audit

volumes:
  audit-data:
